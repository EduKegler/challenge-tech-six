import React from 'react';
interface Repositories {
    repositories: []
}
const Repositories: React.SFC<Repositories> = (props) => {
    return (
        <div className="media" style={{backgroundColor: "#000", height: "100%"}}>
            
        </div>
    )
}
export default Repositories;
